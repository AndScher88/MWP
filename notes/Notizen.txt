MWP-Systems aufräumen
Nur noch auf Artikelverwaltung konzentrieren

controller:
- AricleController

namespace APP\Article

route : controller/action/parameter

In der NAvbar müssen die links geändert werden in zum Beispiel article/show/all
controllerName = article
controllerMethode = show
controllerParameter = all

Die URL kann über $url = $_SERVER['REQUEST_URI']; azusgelesen werden und über explode '/' getrennt werden und in variablen geschrieben werden
Darüber kann dann ein Router geschrieben werden


Autoloader muss überarbeitet werden

